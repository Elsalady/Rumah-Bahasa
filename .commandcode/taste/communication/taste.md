# communication
- When a design decision has several plausible formats/approaches, the user appreciates being presented with concrete named options before implementation — the assistant proposed multiple candidate formats for the member-number scheme (with label, description, and example previews) via a multiple-choice question ("Mau pakai format nomor member yang kayak gimana?"), and the user then described their own hybrid variant of those options (embed date info but keep it subtle) which was implemented. Confidence: 0.55
- Use Indonesian language (bahasa Indonesia) when communicating, not English. Confidence: 0.85
- Communicates in a casual/informal Indonesian tone — slang ("woii", "aduhh"), heavy abbreviations ("gausah"/"ga", "knp"/"cmn"/"doang"), exaggerated spelling ("munculll") — often as very terse shorthand questions (e.g., "knp yang berubah cmn rejected nya doang"), and expects the assistant to match that casual register rather than overly formal language. Confidence: 0.8
- Use Indonesian language (bahasa Indonesia) when communicating, not English. Confidence: 0.9
- Communicates in casual/informal Indonesian (e.g., "haii", "tolong betulkan yah", "habistu aku mau lanjutin") — match with a friendly, informal tone rather than stiff/formal language. Confidence: 0.6
- The user is a beginner/non-expert regarding the project's own tech stack (e.g., asked whether "dbnya pakai pg admin" without realizing pgAdmin is just a GUI tool for PostgreSQL, not the database itself, and feared that `render.yaml` might contain secrets like `.env` does) — explain technical concepts in simple, plain language and gently correct misconceptions rather than assuming technical familiarity. Fresh example: while following an env-var fix they asked whether the key must be uppercase ("key nya ga uppercase?") — even basic env-var naming conventions are unfamiliar and must be stated explicitly. Confidence: 0.8
- The user treats config-file values as authoritative: they opened the placeholder URL written in `render.yaml` (`https://rumah-bahasa.onrender.com`) and reported "not found" without ever having deployed — not realizing that URLs/service names in a deploy config are guesses and the REAL URL is assigned by Render at deploy time (shown in the dashboard). Whenever a guessed/placeholder value is put in a config file, explicitly warn that it is just a placeholder and that the actual value (URL, service name) appears after deployment — otherwise the user will open the guessed URL and think the site is broken. Confidence: 0.65
- The user reports vague symptoms tersely with no context (e.g., "masih not found" after a deployment discussion; "mau login sbg admin gabisa katanya email atau password salah" after creating an admin via tinker — reporting the app's error text but omitting which email/password they used, the account's status, and which database the web service points to) — the most effective diagnosis is to ask structured multiple-choice clarifying questions (which URL are you opening — Render or localhost? what is the deployment status?) and let them pick, or enumerate the likely causes in a prioritized order, rather than guessing or dumping a generic troubleshooting list; this pinpointed the root cause (they had opened the placeholder URL without deploying at all). The same terse pattern continues even mid-diagnosis — the user reports the result of one check with no surrounding context ("sama keduanya hasil dan isi nya railway, tapi kenapa masih tidakbisa login...") — so keep the diagnostic loop to one question/one check at a time and interpret the terse reply as the answer to the last asked check — e.g., after being asked to try incognito, the user replied only "udah incognito tetep gabisa login", which is the result of that one check (incognito failed → browser cookies ruled out), not a new problem. Confidence: 0.8
- When explaining technical concepts, the user explicitly asks to explain "like to a middle-schooler who knows nothing about IT" ("jelaskan kaya kamu jelaskan ke anak smp yang tidak tahu apa apa tentang IT") — use everyday analogies (e.g., cooking/fried-rice-and-kitchen analogies for deploy/server/localhost/hosting/VPS/domain), simple non-jargon wording, and concrete before/after comparisons rather than technical detail. Confidence: 0.8
- When the assistant misinterprets a requirement OR misdiagnoses a bug, the user restates the correction — sometimes loudly in caps (e.g., "GAMBAR NYA 3 AJAAAAA", "ITU TERMASUK LOGO NAH EMANG FILE NYA 5 GAMBAR TAPI YA GAUSAH DITAMPILIN SEMUA KAN"), sometimes quiet and terse ("bukan ituu, data pendftar nya masuk kok ke menu pendaftar, tapi ... box ... yang belum muncul") — when this happens, carefully re-derive the actual intent from the full restatement instead of repeating the previous wrong hypothesis. For a misdiagnosed bug, the correction precisely scopes the symptom to a specific UI surface (here: data exists in menu Pendaftar, only the dashboard "Pendaftar Terbaru" box is empty → a presentation/view-layer issue, not a missing-DB-data issue), so pivot to that exact widget rather than continuing the old line of investigation. FRESH EVIDENCE: when a fix still missed the mark, the user asked point-blank whether the assistant even understood the request ("masih belum berubahh, kamu paham ngga sih yang aku minta") and then restated the requirement as a step-by-step end-user journey (register member → fill nama/email/password → click "Lihat contoh surat" → view the letter → click the back arrow "kembali ke pendaftaran" → entered data must NOT be lost) — re-derive the core requirement from that walkthrough rather than re-applying the previous hypothesis. Confidence: 0.82
- The user will correct a logbook/summary that overstates completion — when work (e.g., a deploy) is still blocked by an open issue, the summary must honestly say it's not finished yet ("tapi kan deploynya belum selesai, masih error") rather than phrasing it as "selesai/menyelesaikan"; even if the code-side work is done, keep the open status explicit so the logbook doesn't claim a done deploy. Confidence: 0.7
- Provide concise summaries of completed technical changes/features for the user's daily logbook (logbook); when asked for a daily recap, the user wants it as a SINGLE paragraph ("1 parafraf aja") of flowing prose covering all that day's work — and may explicitly tighten it to a SINGLE sentence ("tolong kasih logbooknya 1 kalimat aja"), which the assistant should honor by compressing all of the day's fixes into one flowing sentence — never bullet points, sections, or multi-paragraph structure. The user typically requests the logbook when wrapping up at the end of the work session ("oke udah segini dulu hari ini"). FRESH EVIDENCE: on the final request of a session, the user asked again for the logbook as "1 paragraf" that is "singkat jelas" (short and clear) AND explicitly asked that the wording NOT be too fancy/formal ("kata kata nya jangn terlalu kaya mudah") — so write the logbook in plain, everyday Indonesian the user would naturally write themselves, not polished/formal phrasing. Confidence: 0.9
- Logbook requests may be scoped to a SPECIFIC portion of the day's work rather than the whole day ("yang bagian fixing pas deploy tadi kan berkalikali gagal, nah itu aja tolong deskripsiin singkat dan jelas") — when asked, focus only on the named sub-topic. For work that involved repeated failures (e.g., a deploy that failed several times), the user wants the troubleshooting narrative itself described in the logbook — what kept failing (DB connection, port, wrong URL) and that each was fixed one by one until success — still as short, plain, single-paragraph prose, not a list of attempts. Confidence: 0.65
- The user works with a friend/teammate who reviews work in progress, and pauses development at a checkpoint for that review before continuing ("dah ini dulu biar dilhat temanku dulu") — expect occasional stops so someone else can look at/play with the work, rather than always pushing straight through to completion. The external reviewer can also be the user's mentor: the user shows the finished result to their mentor and returns with "tambahan dari mentor" (additional requirements/feedback) to be implemented — when the user opens a session with "ada tambahan dari mentor ku", expect change requests from that external review, ask for the details, and be ready to implement them in the code. FRESH EVIDENCE: mentor-sourced feature requests can also arrive inline mid-session with no preamble — "sama katanya tuh suruh nambahin nomor member, kata mentorku" (the mentor also told me to add a member number) — the user relays the mentor's request verbatim as a to-do and expects it implemented directly alongside the work just completed. Confidence: 0.68
- When the user asks a capability/yes-no question (e.g., "kamu bisa ngedeploy ga?"), they often want ONLY an answer, not immediate action — explicitly flagged with "tanya doang ini jadi tolong dijawab ya" (just asking, so please answer). Answer the question directly, briefly outline what's possible, and wait for a go-ahead before starting any work. Confidence: 0.6
- When the user needs to carry out a multi-step technical procedure themselves (e.g., deploying to Render: "bisa jelaskan atau bantu gak tata cara dari awal sampai akhir hingga berhasil melalui step step detail"), they want a complete from-start-to-finish walkthrough with detailed, numbered steps — covering every step in order (setup, each action, expected outcome, and known limitations) so they can follow it exactly, not just a high-level overview. They re-request this explicitly ("kasih tutorial yg lengkap", "kasih tutorial lengkap sampai akhir bisadeploy", "kasih tutor lengkap cara daftarin akun admin di railway biar bisa buka panel admin") even after receiving earlier guidance. Confidence: 0.95
- When a session/work is interrupted (e.g., the user accidentally stops it: "aduhh ga sengaja ke stop, terus ini lanjutanya gimana"), the user expects the assistant to pick up from the last completed step and finish the remaining in-flight work (here: continue the pre-deploy readiness check for items already agreed on) rather than restarting from scratch or asking to re-confirm the plan. This extends across sessions: returning the next day with a terse status reminder ("haii aku kembali lagii, yuk lanjut deploy, kemaren cssnya belum tampil semua, sekarang gimana"), the user expects the assistant to remember the prior state and resume the in-flight deploy from where it left off without re-confirming the plan or re-explaining context. Confidence: 0.65
- When the user is unsure about a recommended practice, they push back with "wait, is X really safe / why not just Y?" questions (e.g., "loh emang aman ya kalo render.yaml di push di git? kenapa ga taruh di git ignore aja?") and expect the reasoning explained in simple terms (e.g., render.yaml is a recipe/blueprint with no secrets while .env holds real passwords) — not just "trust me, do it this way". Confidence: 0.6
- The user does not understand that a local URL (localhost) only works while the dev server is running — when they open a link and get "not found", they ask whether they need to "start something" or run `php artisan serve` first. When referencing local URLs, always remind them to start the server first (e.g., `php artisan serve` or the project's `serve.bat`), and clearly distinguish localhost from the deployed Render URL before diagnosing the issue. Confidence: 0.6
- The user gets confused by external platform onboarding/verification prompts they encounter mid-workflow (e.g., Render asking to "Add card" when creating a Blueprint) and asks what the prompt means and how to avoid it — explain what the platform is actually asking for (identity verification vs. charging), whether it can be skipped, and give concrete workarounds before assuming they know how to proceed. Confidence: 0.55
- When walking the user through an external platform's UI (e.g., Render/GitHub), explicitly state WHERE each control is located on the page — which menu, button, tab, or section — because the user doesn't know the platform layout and asks for exact locations ("kalo mau milih repository yadimana juga letaknya" = "and where do I pick the repository, where is it located?"). Always pair the action with its on-screen location. Confidence: 0.75
- The user doesn't understand that third-party platforms (e.g., Render) can't see their GitHub repositories until they explicitly authorize it via GitHub App permissions — when a repo doesn't show up in "Connect repository" ("kok di connect repository ga muncul repo rumah bahasa ku"), explain the GitHub App permission model (All repositories vs Only select repositories, and how to reconfigure via GitHub Settings → Applications) with distinct fixes for "empty list" vs "some repos missing", rather than assuming the repo was never pushed. Confidence: 0.6
- When walking the user through filling out a platform form (e.g., the Render Blueprint form: Name, Plan, Branch, Root Directory), give exact values for EVERY field — explicitly state what to type, what to leave blank, and what is auto-filled from config and should not be touched ("di isi apa ini") — using a field-by-field table rather than describing only the actions. Also warn that a post-deploy step (updating APP_URL to the real Render URL) is needed even after the site is live. Confidence: 0.65
- When a platform/deploy step fails, the user pastes the full raw error output verbatim with no framing or interpretation (e.g., "muncul kaya gini : " followed by the entire Render Blueprint validation dump, or "ini ko giniiiiii : " followed by the whole Railway crash log `/bin/sh: 1: [sh,: not found ...`) — parse the specific error codes/messages out of the dump (e.g., "pre-deploy command is not supported for free tier services", "invalid runtime php", `[sh, : not found` from a mis-parsed CMD array) and fix the underlying config directly, rather than asking the user to interpret the errors or re-copy them. The same verbatim-paste habit extends to whole platform UI/settings pages: when asked to check a dashboard, the user pastes the ENTIRE page as raw text with zero framing (e.g., the whole Railway Settings/Deploy tab — source repo, port, builder, custom start command, pre-deploy command, replica limits) and expects the assistant to read the relevant fields out of the dump and spot the misconfiguration directly (here: stale `npm run start` / `npm run migrate` dashboard commands overriding the Dockerfile). The habit also covers raw deployment/startup logs pasted verbatim with minimal framing (e.g., "gini : " followed by the whole Railway container-start log — PHP-FPM notice lines, "Starting nginx on port 8080...", "Running database migrations... Nothing to migrate") as progress reports: the assistant should read the log lines directly to infer deploy state (which entrypoint steps ran, which port nginx bound) rather than asking for clarification. This recurred again when the user pasted an entire clean Railway container-start log ("You reached the start of the range ... Starting Container ... Clearing Laravel caches ... Starting nginx") with no framing at all, as a silent progress report. It recurred yet again with a crash log: the user pasted the whole repeated Railway container-crash dump (`SQLSTATE[08006] could not translate host name "dbname='laravel'" ... Clearing Laravel caches ... FAIL`) framed only by "ini kenaakok crash" — read the specific failing line out of the dump (here: pgsql Host empty → DB env vars missing) and fix the config, don't ask them to interpret it. Confidence: 0.9
- When handing back after a fix/check, proactively give practical run-advice tied to the user's actual environment (e.g., after `.env` changes, tell them to close stale terminals / start a fresh session because a shell session can hold old env vars) — a small closing advisory so the fix actually takes effect when they run the app themselves. Confidence: 0.6
- When the user switches deployment platforms mid-project (e.g., moving from Render to Railway after "aku udah push ke github dan sudah deploy ke railwar"), the assistant should adapt the existing deploy setup for the new platform and still give the platform-specific manual steps (set env vars in dashboard, attach a PostgreSQL database, redeploy) — platform migrations are a recurring pattern, not a one-off. Confidence: 0.6
- When the user reports a deploy bug, they may point at a specific file with a guessed cause (e.g., "file resources/views/home/index.blade.php, kamu cek karena saat dideploy ada css yang tidak terkonfigurasi") — use the named file as the investigation starting point but do NOT accept the stated diagnosis at face value: here the blade file and the CSS were actually fine (200 OK, committed, matching HEAD) and the real cause was stale cached HTML at the edge. Verify actual render/serving state before editing the file the user suspects. Confidence: 0.55
- The user does not understand that a deployed production site and the local dev environment (VSCode) are two separate applications with separate databases — they expected a member registered via the deployed website (from their phone) to appear in the local admin panel, and expected locally-created accounts (member + admin) to work on the deployed site. When a cross-environment data question comes up, first explain the "two worlds / two databases" model (deployed DB on the server vs. local DB on the laptop) with a comparison table, then clarify that data and accounts must be created in the database of the environment where they will be used (e.g., create the admin in the Railway database via the platform's Shell, not locally) — this is not a bug, just separation between environments. The user acted on this model by immediately requesting a full tutorial to create the admin account inside Railway (not locally), confirming they internalized the two-database separation. FRESH EVIDENCE: the pattern recurred when the user asked to fix registrant data "belum muncul di box pendaftar baru di menu dashboard" on the deployed admin — the dashboard code was fine and the local DB had 7 pendaftaran records, so the diagnosis had to pivot to checking the Railway database (via the platform Shell), not the laptop. When a UI box is empty on the deployed site, first verify the data exists in the *deployed* database before touching code. Confidence: 0.8
- The project is developed as teamwork across multiple laptops: a teammate deployed the project on their own machine ("yang deploy temen ku di laptop nya") and the user pulled the code to continue the work ("hari ini ku pull ku lanjutin teamwork kita ini") — expect a multi-machine, git-shared workflow where code, local databases, and deployed state may differ between contributors until pulled/pushed. Confidence: 0.6
- When the user is confused about how deployment actually works (e.g., "sebenernya sistem nya ini gimana sih?"), they want the conceptual model explained first (how the deployed site, local dev, and databases relate) rather than just a list of fixes — answer the "how does this work" question before moving to concrete steps. Confidence: 0.65
- The user double-checks platform terminology when instructions use a different name than what they see in the UI (e.g., on Railway they asked "eh shell itu bener console kan?" — whether the "Shell" tab is the same as "Console") — when a platform has multiple names for the same feature (Shell/Console/Terminal), proactively confirm the aliases are equivalent and reassure them they're on the right screen before continuing. Confidence: 0.55
- The user executes provided multi-line terminal commands exactly as written — typing each line and pressing Enter individually, following exit shortcuts like Ctrl+D — so provide complete copy-paste-ready command blocks (including exit steps), not abbreviated pseudocode. When a fix has been followed exactly and still fails, the user reports back tersely that they did everything as instructed ("udah kuketik perintah ... tapi tetep gabisa"; "tetep gabisa stelah aku ganti ke file dan udah ke save dan deploy tetep gabisa login") — pivot to a verification step that prints concrete state (e.g., a tinker one-liner echoing the connected DB name, admin email/status, and a `password_verify` result) and ask them to paste the output, rather than repeating the same fix, re-explaining, or questioning whether they executed it correctly. When the config-level fix (e.g., SESSION_DRIVER change) also fails, the next pivot is to inspect the application code itself (routes/middleware/AppServiceProvider) for something blocking the login flow (e.g., an unregistered throttle middleware or `URL::forceScheme('https')` breaking redirects) and check the platform logs. Confidence: 0.72
- The user does not understand interactive REPL/terminal semantics: shown a multi-line `php artisan tinker` snippet, they asked whether the `;` marks where to press Enter or whether to type everything in one line ("apakah per tanda ; itu ditekan enter, atau semua itu ku ketik baru enter?") — typing line-by-line worked, pasting all lines at once errored. When giving tinker/REPL commands, explicitly state that EACH line is a separate command executed immediately on Enter, tell them NOT to type the semicolons, and prefer single-line one-liner expressions (e.g., `App\Models\User::where('role','admin')->get(['email','status'])`) over multi-line statement blocks so there is no ambiguity about where one command ends. FRESH EVIDENCE: the user once typed a PHP tinker statement directly at the bash prompt (outside tinker) and got `bash: syntax error near unexpected token '/login','` — they then double-checked "ini ga di dalem tinker kan ngetiknya?" — so every tinker instruction must explicitly say the PHP code goes INSIDE `php artisan tinker` (enter tinker first, then type each line, then exit with Ctrl+D), never at the bash shell prompt. FRESH EVIDENCE (cont.): even inside tinker, a long fully-qualified class path (`Illuminate\Http\Request::create('/login','POST',[...])`) was mistyped as `Iluminate` ("di tinker jg gabisa, hasilnya eror class Iluminate not found") — long namespaced class references are typo-prone for this user, so prefer short Laravel global helpers (`request()->create(...)`, `app()->handle(...)`, `Auth::login(...)`) in tinker one-liners, and on any tinker error pivot to those shorter helpers rather than re-explaining the class path. Confidence: 0.8
- When a login/data issue on a deployed app resists diagnosis, the user reliably executes backend verification one-liners in the platform Shell (tinker) and reports only the single result word ("hasilnya true") — treat a bare `true`/`false` reply as the direct answer to the last verification command asked, and when the backend checks ALL pass (DB name matches, password verify true, Auth::attempt true) yet the browser login still fails, pivot decisively to the remaining client-side/deploy-session layer instead of re-running more backend checks. In this episode Auth::attempt=true proved the app-side login works, isolating the fault to the browser session layer (SESSION_DRIVER=database) — the fix moved to deploy config, not the application code. Confidence: 0.7
- The user cannot distinguish routine operational log entries from real errors — they pasted a normal PostgreSQL checkpoint line (`LOG: checkpoint complete ...`) framed as "ini eror paling akhir" (this is the latest error). When the user pastes log output labeled as an error, FIRST triage whether the line is actually an error at all (routine DB checkpoint/cron/startup noise vs. a real failure) and state plainly what the line means (e.g., "that's just routine database housekeeping, not an app error") before continuing the diagnosis — don't treat every pasted log line as evidence of failure. Confidence: 0.6
- When a browser-login failure survives the incognito test on a deployed app ("udah incognito tetep gabisa login"), that single result rules out browser cookies/cache — the user then expects the assistant to pivot deeper into the deploy-side session layer (APP_KEY set/consistent, SESSION_SECURE_COOKIE forcing https-only cookies, APP_URL correctness) after previously ruling out the app code (middleware/redirects) and DB driver changes (SESSION_DRIVER=file); the pattern is: each negative result from one diagnostic layer narrows the hunt to the next layer rather than restarting or repeating fixes. Confidence: 0.6
- The user reports config-mismatch findings with mild self-blame and relief ("yaAllah tibake app url e kok onrenderr" — "omg it turns out the app url was onrender") when a diagnosis finally lands — the immediate follow-up to a confirmed root cause is a concrete, ready-to-apply fix (exact env var + where to change it in the platform dashboard + expected outcome after redeploy), delivered with an excited aha framing that validates the find. Keep this cadence: celebrate the confirmed root cause, then give the exact change. FRESH EVIDENCE: the user found the wrong-link root cause with the same excited all-caps relief ("WALAHHH TERNYATA LINK NYA BEDA SAMA YANG DARITADI TAK COBA ANJAYYY BEHH") and the assistant's "HAHAHA ketemu kan! 😄" aha framing was well received, after which the user immediately moved to the next symptom on the correct URL. Confidence: 0.6
- When an already-applied fix still doesn't resolve the issue, the user vents frustration with profanity and all-caps slang ("ANJERRRRR TTEP GABISA MASUK" — still can't get in after the APP_URL change) — the effective response is to stay calm and reassuring ("Santai, kita pasti beres!"), then verify whether the change actually landed (value saved correctly, manual redeploy, Laravel config cache cleared) with ordered steps rather than repeating the same fix or questioning whether they executed it. Confidence: 0.6
- The user asks the assistant to confirm the exact values they plan to enter (e.g., "BENER GASI URL NYA https://rumah-bahasa-production.up.railway.app" — is this the right APP_URL?) — explicitly validate the value, and when it can't be verified from code alone, give the precise dashboard location to find the authoritative value (Railway: service → Settings → Networking → Public Networking) plus format gotchas (no trailing slash), rather than just saying "yes that's right". Confidence: 0.6
- When a previously-identified root cause (e.g., wrong APP_URL) is fixed but the symptom persists, the assistant's first move should be to walk through verifying the change actually landed — URL value saved without typos/trailing slash, manual redeploy until status "Deployed", and Laravel config cache cleared in the platform Shell — in that exact order, rather than assuming the diagnosis was wrong and restarting the hunt. Confidence: 0.6
- When reporting a bug, the user gives a precise play-by-play of exactly which control was clicked and exactly which data was lost or retained (e.g., the register-form sample-letter popup: closing with the "X" kept everything, but clicking the arrow-with-text "Tutup" lost only the uploaded document while text fields survived) — parse that precise click-path and the exact scope of what survived vs. was lost to pinpoint the mechanism (here: the "Tutup" arrow was a link that navigated/reloaded the page → file input wiped, text restored from localStorage), rather than re-hypothesizing from the vague first report. Confidence: 0.55
- When instructing the user to edit an environment variable in a platform dashboard (e.g., correcting APP_URL on Railway), explicitly confirm the exact key spelling (all-caps with underscores — `APP_URL`, not `app_url`/`App_Url`, since Laravel reads env names case-sensitively), whether to edit the existing row's value in place or delete-and-recreate it, and format gotchas (no trailing slash, no trailing spaces) — the user paused mid-procedure to ask "bentar ... berarti aku hapus variable APP_URL? ... key nya ga uppercase?" and needs these conventions stated explicitly before executing, not assumed. Confidence: 0.6
- The user does not understand the code-change → git push → deploy pipeline: after the assistant edits code, they asked whether changes auto-push to GitHub / auto-deploy or whether they must push manually ("apakah aku perlu push manual ke github atau udah otomatis, dan deploy nya juga apakah manual atau otomatis"). When explaining this workflow, give an explicit per-step breakdown of who does what and which steps are manual vs automatic (assistant edits locally → USER commits & pushes manually with the 3 commands `git add .` / `git commit -m "..."` / `git push origin main` → Railway auto-detects the push and rebuilds/deploys in ±3-5 min), because the user conflates "assistant changed the code" with "already deployed/updated". Confidence: 0.6
- The user performs their own systematic client-side testing before reporting failure — after being asked to try incognito, they additionally tested the same tab, different tabs, and different browser apps on their own initiative ("udah tak lakuin semua cara yang kamu suruh, trus tak coba selalu di tab yang sma dan berbeda di apk browser yg beda jugaaa tapi kenapa tetep gabisaaaa login sbg adminnnn"). When they report "did everything" after this self-administered cross-browser due diligence, accept that the client/browser layer is exhausted and stop guessing — pivot to a server-side probe that replicates the browser's full HTTP request (e.g., `Illuminate\Http\Request::create('/login','POST',[...])` run through `app(Kernel)->handle($req)`, then check status code + Location header) to isolate request-layer failures (CSRF/throttle/session) from app logic, while explaining in plain terms why a direct tinker call (`Auth::attempt`) can succeed even when the full HTTP request path fails. Confidence: 0.6
