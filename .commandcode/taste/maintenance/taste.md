# maintenance
- Leftover Git merge-conflict markers (e.g., `<<<<<<< Updated upstream` / `>>>>>>> Stashed changes` blocks left in tracked files, such as blade views) should be resolved/cleaned up whenever encountered — they break the rendered output (e.g., swallowed a "Jadwal Kelas" footer link) and must not be left in the codebase. Confidence: 0.6
- Remove dead/unused files (code files, views, duplicate files) AND orphaned directories/folders from the project entirely rather than leaving them in place — prefers a clean codebase without orphaned files or leftover folders. Confidence: 0.85
- Remove unneeded features and menu items entirely rather than keeping them (e.g., removing the edit-berita action from the admin konten panel, removing the Galeri link from the burger menu, removing the whole email-based forgot-password feature once it required too much admin configuration, and removing the reject/approve UI for program enrollment — admin dropdown, update route, and controller method — once enrollment became instant) — prefers a lean feature set where only necessary actions and navigation are exposed, with no leftover "hidden but present" controls. Confidence: 0.75
- Clean up unused database tables (Laravel default tables like sessions, cache, jobs that aren't used by the application) as part of project maintenance to keep the database schema focused and minimal. Confidence: 0.75
- Proactively identify gaps and missing features across the entire project (public website, admin panel, member area) and suggest/add improvements, not just follow the literal request — expects thoroughness and completeness checking. Confidence: 0.8
- When the user asks to check "semuanya" (e.g., "tolong cek semuanya... dari pas orang mau lihat websitenya, daftar member, registrasi, admin, acc, buat jadwal... alurnya"), they want a systematic end-to-end audit of the complete user flows across ALL roles — visitor/public pages, member registration & approval, member area, admin approval & schedule management — not just spot checks of individual features. Verify each flow end-to-end (including authenticated member/admin sessions via HTTP smoke tests) and report results per flow, fixing bugs uncovered during the audit (here: a leftover legacy `member_code` system referencing a DB column that never existed caused 500s on several admin pages, consolidated to a single `no_member` source of truth). Confidence: 0.6
- Verify new features end-to-end against the real system before declaring them done — exercise the actual HTTP flow (submit forms with CSRF token, confirm records in DB, read the mail log for sent emails, verify resulting state like logging in with the new password), not just syntax checks. The user tests delivered fixes in the real browser and reports back (e.g., "aduhh tetap ga muncul dropdown nya") when a change was declared done but doesn't actually work — so a fix should only be called "beres" after it is confirmed working in the actual rendered page — the user later reported "menu program dan jadwal tidak bisa dibuka" even though the affected pages had passed a tinker render test, confirming render-only checks can miss real breakage. Confidence: 0.82
- After testing that mutates real records (e.g., changing an admin's password via the reset-password flow), restore the original values/data so the system returns to its pre-test state — the user explicitly asks to revert test side-effects on real data rather than leaving them in place. Confidence: 0.65
- After fixing an error/bug, verify the whole site still works before reporting completion — smoke-test all public pages (200 OK) and protected pages (redirect to login) over HTTP and confirm no new error entries in the Laravel log; only then declare the fix done and hand back to the user. A fix to one area can break a different page (e.g., after the konten/profil fix, the admin "Program & Jadwal" menu no longer opened), so re-check related pages, not just the one touched. Confidence: 0.68
- The user works on Windows (cmd shell) — avoid Unix-only shell features: the `grep` shell command is unavailable (use the built-in grep tool) and heredocs like `<<EOF` are unsupported (use multiple `git commit -m` flags instead). Confidence: 0.9
- On this Windows + PowerShell setup, `php artisan tinker --execute="...$b..."` breaks because PowerShell interprets/mangles `$` — when a query needs PHP variables, write the snippet to a scratchpad `.php` file and run `php artisan tinker <file>` instead of fighting PowerShell escaping. Confidence: 0.6
- In production, `APP_DEBUG` must be `false` (so errors don't leak stack traces / sensitive info) and `APP_URL` must be set to the real domain (so all generated links — redirects, emails, sitemap — use the correct domain); `.env.example` and the deployment guide should encode this with clear comments rather than leaving dev defaults. Confidence: 0.9
- During whole-project health checks, also verify `.env` branding/locale drift: `APP_NAME` must match the real brand (a typo "Rumah Belajar Surabaya" was corrected to "Rumah Bahasa Surabaya") and `APP_LOCALE` should be `id` for Indonesian-consistent date/time formatting — cosmetic, but they surface in mail subjects and formatted output. Confidence: 0.6
- When diagnosing an erroring admin menu/page, fail fast with the cheap deterministic checks before reading feature code — here `php artisan migrate:status` exposed two pending `no_member` migrations (columns missing → query error), then `php -l` on the controller exposed a PHP syntax error (`]);` instead of `];` in `$updateData`), and only then did the full file read reveal nested git conflict markers as the deeper cause. Run migrate:status → php -l → route:list → tinker query in that order; each cheap check either fixes the issue or narrows to the next layer (schema → syntax → unresolved merge markers) without wasted edits. Confidence: 0.6
- When `.env` edits don't take effect even after `php artisan config:clear`, suspect OS/session environment variables overriding `.env` (Laravel prioritizes process env vars over the file) — on this Windows setup, leftover `APP_NAME`/`APP_LOCALE` vars in the shell session masked corrected `.env` values and kept showing the old values in `php artisan about`. Diagnose by checking `[Environment]::GetEnvironmentVariable(name,'User'/'Machine')` and clearing in-session vars (`$env:APP_NAME=''`) before concluding the file is wrong; tell the user to open a fresh terminal if a session holds stale env values. Confidence: 0.65
- Before deploying, the user wants a full production-readiness audit first ("ini kira kira kurang apa aja ya?") — everything finished before deploy, then user testing. The audit should cover .env/APP_DEBUG/APP_URL, storage link, scheduler cron setup, custom 404/500 error pages, default admin credentials, and populated content data, with findings categorized by urgency (already safe / must-fix before deploy / manual user-testing checklist) and prioritized recommendations. (The SMTP mail-config item was later removed from the audit when the user dropped the email-based forgot-password feature entirely.) Confidence: 0.6
- Config template files like `.env.example` should be made consistent with the actual environment stack actually used (e.g., PostgreSQL instead of the SQLite default), so that someone deploying by copying `.env.example` verbatim doesn't silently drift to a different setup (database, mailer) than the working local/production config. Confidence: 0.7
- When updating config template/example files (like `.env.example`), write neutral provider-agnostic defaults with clear inline comments and commented-out example values for each option (Gmail App Password, Mailtrap, etc.) rather than baking in one specific provider — the deployer fills in their own values, and dev-vs-production differences (e.g., MAIL_MAILER=log locally) are called out explicitly in comments. Confidence: 0.7
- When documenting deployment steps (e.g., SMTP setup in the deployment guide), insert the new section in the correct numbered position and renumber all following sub-sections so the document's numbering stays consistent — numbering drift after insertion should be caught and fixed. Confidence: 0.75
- Default credentials/secrets must not ship with weak hardcoded values (e.g., the seeder's `admin123` password) — the user treats changing default admin credentials as a must-fix before production. Prefer env-var-driven secrets (e.g., `ADMIN_PASSWORD`) with a random fallback, `firstOrCreate` in seeders to avoid duplicates, and documented password-change steps in the deployment guide. Confidence: 0.7
- When the user pauses work for someone else to review/try the app (e.g., a teammate), proactively suggest committing the current changes first so there is a safe checkpoint — before letting anyone else look at or poke around the working directory. Confidence: 0.55
- When refactoring a controller's data structure (e.g., changing the `groupBy` key of a collection from ID to name), update every view that consumes that collection in the same change — views still reading the old key (e.g., `$allJadwal->get($prog->id)` after keys became `nama_kelas`) silently render empty/broken sections (here: the admin "Program & Jadwal" menu showing no schedules). Search for all consumers of the changed variable before finalizing a refactor. Confidence: 0.62
- When a user-facing identifier/numbering format changes (e.g., member numbering from per-row/per-day counters to a persistent code like `RB-DDMMYY-NNN`), apply it consistently on EVERY surface that displays that data — the user explicitly scoped such changes to "baik itu di halaman member maupun halaman admin" (both member pages AND admin pages): member dashboard, member edit page, admin kelola/index tables, admin show/detail, JS-rendered drill-down tables, and CSV exports. Replacing all raw `$i + 1` row counters and fallback IDs ("ID Member #id") with the new code everywhere (including the controller-serialized JSON feeding JS tables) is expected, not just on one page. Confidence: 0.8
- When the user reports that a change appears incomplete or only affected one case (e.g., "knp yang berubah cmn rejected nya doang" — why did only the rejected status change), the user expects the assistant to first diagnose the root cause (check the actual data/records in the database) rather than assume the implementation is wrong — the fix can be correct while the data simply only contains one status value (e.g., only `rejected` members exist, so only the red badge shows). Diagnose before re-editing code. Confidence: 0.6
- When a color/style change made in blade templates still doesn't appear on the rendered admin pages (user: "tetepp ga berubahhh warna nyaaa, yang pending blom kuninggg" — the inline pending-yellow badge color still not showing), the root cause can be a global `!important` CSS override in the shared layout — `layouts/admin.blade.php` contained a dark-theme rule `span[style*="border-radius:50px"] { background:#21262d !important; color:#c9d1d9 !important; }` that force-neutralized ALL status badges to monochrome gray, silently overriding the inline pending-yellow/approved-blue/rejected-red colors (only red showed because a separate rule re-colored it). Before re-editing templates or blaming browser/view cache, grep the shared layout for neutralizer/`!important` rules targeting the same elements — inline styles in blade files are not guaranteed to win against layout-level `!important` rules. Confidence: 0.7
- The user chose Render (free plan) as the deployment platform for this Laravel + PostgreSQL project ("aku mau pakai render") — future deployment work should target Render's free tier (Blueprint/render.yaml, PostgreSQL on Render, git-push-triggered rebuilds, and free-plan caveats like the app sleeping after inactivity) rather than paid hosting. Confidence: 0.7
- When deploying to Render, the user wants the assistant to prepare everything directly in the project (not just give instructions): create `render.yaml` in the repo (web service + PostgreSQL, `QUEUE_CONNECTION=sync` since the free plan has no background workers, `MAIL_MAILER=log`, `APP_TIMEZONE=Asia/Jakarta`, `APP_LOCALE=id`) and run a pre-push readiness check (.gitignore covers .env/vendor/node_modules, composer.json PHP version, pgsql support on the platform, storage link, .env.example) so the user only has to git push and click Apply on Render. IMPORTANT (learned from an actual Render Blueprint validation failure): Render's free tier rejects `runtime: php` ("invalid runtime php") and `preDeployCommand` ("pre-deploy command is not supported for free tier services") — the Blueprint must instead use `runtime: docker` with a multi-stage Dockerfile (composer:2 stage, then php:8.3-fpm stage installing pgsql/zip extensions + nginx, `.docker/nginx.conf` for the Laravel entry at /app/public, storage:link run inside the Dockerfile) plus a `.dockerignore` excluding .env/vendor/node_modules; no preDeployCommand in render.yaml. Confidence: 0.7
- Any guessed URL in `render.yaml` (e.g., `APP_URL: https://rumah-bahasa.onrender.com` before the service exists) must be flagged explicitly as a placeholder — Render assigns the real URL at deploy time and shows it in the dashboard, and the user WILL open the guessed URL and report "not found" thinking the site is broken (this happened). Include the "buka URL dari dashboard, bukan dari file" instruction in every deploy walkthrough. Confidence: 0.65
- The user is security-conscious about git: worries that pushing files to the repo (e.g., `render.yaml`) could leak secrets (DB credentials, API keys) the way `.env` would ("aku takutnya ada db db tertentu kaya api key dll kaya env") — expects an explicit reassurance/explanation of which files are secret-free and safe to commit (config/blueprint files like render.yaml contain only settings, with `APP_KEY` generated at deploy time) versus which must stay gitignored (`.env` with real passwords), before proceeding with the push. Confidence: 0.7
- The user is cost-averse and does not want to provide a credit/debit card even for a "free" platform signup — when Render's Blueprint flow demanded an "Add card", they asked what it meant and how to proceed without one ("gimana caranya biar gapake add card"). Deployment guidance should explain that free plans only use the card for verification (no charge while staying on the free tier), check for a skip-without-card option, and offer genuinely card-free alternatives (e.g., Indonesian hosting paid via QRIS/transfer, virtual debit cards) rather than assuming they'll accept a card requirement. Confidence: 0.6
- When the user reports that members can't log in / can't access the member area (e.g., "ini kenapa ko aku ga bisa masuk ke member"), don't assume the auth logic or middleware is broken — the root cause here was a PENDING MIGRATION: the member dashboard 500'd with `Undefined column: "jenis"` because `add_jenis_mode_jadwal_to_pendaftaran_table` was still Pending, so columns the controller queries (`jenis`, `mode`, `jadwal_id`) didn't exist in the database. Diagnose access/login complaints by reading the Laravel log for the actual SQL error and running `php artisan migrate:status` (then `php artisan migrate --force`) before re-editing auth code — the user's database can drift from the migrations in the repo, making login "fail" with a 500 after authentication succeeds. FRESH EVIDENCE: the same root cause broke an ADMIN menu — the admin "Member" menu errored because two `2026_08_31_*` migrations (`add_no_member_to_users_table`, `update_no_member_format`) were still Pending, so `no_member` didn't exist while models/views/controller already referenced it. So a pending-migration check applies to ANY menu/page error (admin or member), not just login: run `migrate:status`, apply pending migrations, then verify with `php -l` on the controller and a tinker query before touching feature code. Confidence: 0.65
- The user has moved deployment to Railway (they pushed to GitHub and deployed there, asking "cara deploy di railway tuh piyee") after earlier Render work — deployment guidance must cover Railway: prepare all repo-side config in the project (a new `railway.json` forcing the Dockerfile builder `"builder": "DOCKERFILE"`) so the user only has to push and set dashboard env vars, because Railway's default Nixpacks runs Laravel with the wrong document root and `public/` assets (css/js) 404. Confidence: 0.75
- When diagnosing missing CSS/assets on a PaaS deploy, don't assume the build output is the problem — first check how views reference assets (here all views use `asset('css/style.css')`, not `@vite`/`public/build`) and whether the files are actually tracked in git and served by the platform's document root: `public/css/style.css` was tracked and fine, while the real causes were Nixpacks not serving `public/` as document root and `APP_URL` not set to the platform domain, making `asset()` generate wrong URLs. Confidence: 0.6
- For container deploys on platforms that inject a `PORT` env var (Railway), the nginx config must listen on that port. CORRECTION (fresh failure evidence): envsubst (gettext) substitutes only plain `$PORT`/`${PORT}` — it does NOT understand shell-default syntax, so `listen ${PORT:-80};` is left verbatim and nginx crashes with `invalid port in "${PORT:-80}"`. The working pattern: nginx.conf uses plain `listen $PORT;`, the default is applied in the shell BEFORE envsubst runs (`export PORT=${PORT:-80}`), then `envsubst '$PORT' < default.conf > /tmp/nginx.conf && mv` at container start; also run `php artisan migrate --force --no-interaction` at container start so a fresh PaaS database gets the full schema (including later-added columns like `jenis` that previously caused errors). Confidence: 0.75
- For Railway Docker deploys, the Dockerfile `CMD` must be written in shell form (a plain string: `CMD php artisan ...`) rather than exec-form array (`CMD ["sh","-c","..."]`) — Railway reads the CMD array literally and executes it as the string `[sh, -c, ...]`, producing a crash loop of `/bin/sh: 1: [sh,: not found`. This bit the project when a previous commit introduced `CMD ["sh","-c",...]` (Render tolerated it; Railway did not) — keep the CMD as a single quoted shell command string for Railway compatibility. Confidence: 0.7
- On Railway, an existing service's dashboard can also hold a stale Pre-deploy Command (`npm run migrate`) alongside the Custom Start Command — both override the Dockerfile and both must be cleared (Settings → Deploy). Adding matching `migrate`/`start` scripts to package.json is NOT a working fallback because the app-stage Docker image (`php:8.3-fpm`) has no Node/npm, so `npm run start` fails with "npm: not found" even with valid scripts — the definitive fix is emptying both dashboard fields so the Dockerfile ENTRYPOINT runs. Confidence: 0.7
- Existing Railway services keep the Start Command they have stored in the dashboard, and `railway.json` pushed from the repo does NOT override dashboard-stored settings for an already-created service — when fixing a broken/legacy start command (e.g., the `[sh, -c, ...]` artifact stored from an old deploy), tell the user to clear it manually (Railway dashboard → service → Settings → Deploy → Start Command → empty it) AND keep `railway.json` free of a `startCommand` field so the Dockerfile CMD remains the single source of truth. Railway start command overrides the image CMD, so when repeated failed deploys show the CMD/entrypoint code never executing (e.g., nginx errors persisted even after fixing envsubst in CMD), the bulletproof fix is to move the whole start sequence (migrate → envsubst → php-fpm → nginx) into an `ENTRYPOINT` script (`ENTRYPOINT ["/entrypoint.sh"]`) — Railway's start command replaces CMD but cannot override ENTRYPOINT (it merely becomes `$@` args the script ignores), so dashboard-stored junk can no longer break the start. Confidence: 0.75
- When a deployed page renders HTML that does NOT match the committed view source (e.g., missing inline hero/gradient styles) even though git is clean, the view file is committed, dynamic data is fresh (newly-seeded records DO appear), and CSS/assets return 200, the cause is stale cached HTML — not the blade file or CSS the user suspects. On Railway this was the Edge "CDN Caching" option caching HTML (DB-driven content stays fresh while the HTML stays old). Verify with cache-busting query strings (?v=2, ?v=3) and direct the user to disable/clear CDN caching, hard-refresh, and test in incognito before re-debugging the code. Confidence: 0.6
- The container entrypoint should also run `php artisan optimize:clear` and an idempotent `php artisan db:seed --force --no-interaction` (seeders use firstOrCreate) at start, before migrate/php-fpm/nginx — clearing compiled-view caches guards against stale Blade, and seeding guarantees a fresh PaaS database isn't missing data-dependent sections (here an empty `layanan` table made the homepage "Lingkup Pelatihan" section render empty even after migrations ran). When runtime-only clearing proved insufficient (stale HTML persisted), the pattern was hardened to `rm -rf storage/framework/views/*.php` right before `optimize:clear` in the entrypoint AND `view:clear`/`config:clear` in the Dockerfile build stage, so neither a runtime cache nor a built image can serve compiled views from an older commit. Seeding must ALSO be data-guarded, not just seeders-idempotent: run `db:seed` only when the table is empty (e.g., `if (Layanan::count() === 0)`) and clean up existing duplicates first — on Railway (serverless scale-to-zero) the entrypoint re-runs on every restart, and a non-idempotent seeder (`Profil::insert()`) stacked duplicates 5–10× ("Gambaran Umum" ×5, "Visi & Misi" ×10 on /profil). Guard the seed call AND delete duplicates (keep the lowest id per judul) in the entrypoint. Confidence: 0.75

- When `CACHE_STORE=database` (as set on Railway), the `cache` and `cache_locks` tables must exist in the PaaS database — this project's default `0001_01_01_000002_create_jobs_table.php` does NOT create them, so `php artisan optimize:clear` fails with `SQLSTATE[42P01]: relation "cache" does not exist` and cache-backed features (login/register rate limits) 500. Add an explicit migration creating both tables so the database cache driver works after `migrate --force`. Confidence: 0.8

- Force HTTPS for generated URLs/assets in production (`URL::forceScheme('https')` in AppServiceProvider when `app()->environment('production')` or `request()->isSecure()`) — without it, `asset()` emits `http://` URLs whenever APP_URL is unset/http, and browsers block the CSS/JS as mixed content when the user opens the site via https (symptom: "cssnya belum tampil semua" despite the stylesheet returning 200). This keeps asset URLs correct even before APP_URL is set in the dashboard. Confidence: 0.75
- When Laravel reports "Nothing to migrate" during a deploy, interpret it as good news — the database already has all tables (e.g., a teammate ran migrations during an earlier deploy) — and pivot to the next required step (verify/create the admin account via tinker) rather than treating it as a failure; migration guidance should first check whether migrations already ran before advising to run them again. Confidence: 0.55
- When the user must type credentials manually (e.g., logging into a deployed app, where the random generated password "gabisa di salin" — can't be copied/typed), they prefer a simple, memorable, typeable password and explicitly request a specific value ("gimana cara ganti nya biar bisa di ganti jadi admin123") — honor the requested value (e.g., set the admin password to exactly `admin123`) rather than proposing another random string. This coexists with the earlier rule that shipped/default credentials must not be weak hardcoded values: the weak value is acceptable only when the user themselves chooses and sets it. Confidence: 0.55
- When a login on a deployed/migrated app fails in the browser even though backend checks all pass (Auth::attempt true, password_verify true, incognito also fails, no app errors in logs), the decisive check is the `APP_URL` env var — here the old Render placeholder (`https://rumah-bahasa.onrender.com`) was still set on Railway, so Laravel redirected post-login to a dead URL and the user saw "cannot login" despite correct credentials. When the user switches platforms (Render→Railway), env vars do NOT migrate themselves: verify every carried-over value (APP_URL, SESSION_DRIVER, etc.) against the new platform's real domain as part of the diagnosis, before touching app code. Confidence: 0.7
- When a deployed-app login (or any page) fails through many diagnostic rounds while backend checks keep passing, also verify which URL the user is ACTUALLY opening in their browser — here the login failure survived APP_URL, permission, and SESSION_DRIVER fixes because the user had been browsing to a wrong/stale URL (from memory/chat) the whole time, until they discovered "WALAHHH TERNYATA LINK NYA BEDA SAMA YANG DARITADI TAK COBA" (turns out the link was different from what I kept trying). The definitive instruction is to have the user open the EXACT URL shown under the platform's Public Networking section (Railway: service → Settings → Networking → Public Networking) and confirm what they see there, rather than assuming the URL they report testing is the real one. Once on the actual URL, a NEW symptom may surface (e.g., "Application failed to respond" = container crash-loop / port / still-starting issue), so the diagnosis resumes against the real URL instead of re-running checks against the stale one. Confidence: 0.65
- When account data checks pass (email, status `approved`, and `password_verify` all true) yet login still fails, the next diagnostic step is to test the login mechanism itself directly — run `Auth::attempt(['email' => ..., 'password' => ...])` in tinker, which returns `true`/`false` and cleanly separates "credentials/storage are fine, session/web layer is the problem" from "the auth flow itself is broken" (e.g., a database `sessions` table issue with `SESSION_DRIVER=database`), without needing browser round-trips. Prefer such a single deterministic tinker expression over multi-step browser debugging. Confirmed working in practice: the user ran it, reported "hasilnya true", and that single result conclusively isolated the fault to the browser session layer (prompting the `SESSION_DRIVER=database` → `file` deploy-config fix) — when ALL backend checks (DB name, password_verify, Auth::attempt) pass but browser login still fails, stop re-checking backend state and move to the client-side/session/config layer. Confidence: 0.75
- After an env fix (e.g., APP_URL corrected) still doesn't take effect on a PaaS deploy, the next layer to check is Laravel's config cache — `php artisan config:clear` (plus cache/route/view clear) in the platform Shell is the expected follow-up, because a cached config can keep serving the old APP_URL even after the dashboard env var was updated and redeployed. Confidence: 0.6
- When a Railway deploy shows the platform's generic "Application failed to respond" page in the browser, triage it as a CONTAINER-level problem, not app code: the container is either still starting, crash-looping, or not listening on the port Railway expects. The assistant's checklist (which the user accepted and followed): check the service status (Deployed vs Crashing vs Building), read the Deploy logs AND Runtime logs for the actual error, verify the start command listens on the injected `$PORT` (Railway: Settings → Networking shows the expected port, typically 8080/8000), and test `php artisan serve --host=0.0.0.0 --port=$PORT` in the Shell — then ask the user to paste the runtime logs verbatim rather than guessing which of the three causes it is. This symptom is likely to recur on this project's free-tier Railway service. Confidence: 0.55
- When giving git workflow instructions, the user's established habit is plain `git push` (push to the default remote/branch with upstream tracking set up), not the more explicit `git push origin main` — when the assistant's instructions used the verbose form, the user asked whether it was required ("harus git push origin main kah?, biasanya aku git push doang") and expected confirmation that their shorter habit is fine. Use `git push` (or explicitly note both are equivalent) rather than insisting on `git push origin main`; reserve `git push -u origin main` for the one-time "no upstream branch" case. Confidence: 0.7
- The user prefers to accumulate changes and push in batches rather than pushing after every individual fix/feature — "ntar push ku kalo udah banyak yang keubah" (I'll push later once a lot has changed). Don't urge a git push after each small change; instead let the changes pile up, and when the batch is ready suggest a single combined commit message covering all accumulated changes (e.g., "catatan wajib saat reject member, dashboard tampilkan member terbaru"). Confidence: 0.6
- When a page/feature references an asset path that doesn't exist (e.g., an image path `img/contoh-surat-domisili.jpg` that 404s and is silently hidden by `onerror`), locate the file that actually exists in the project (list `public/images` first) and repoint the reference to it (e.g., `images/contohfile.jpeg`) — the user explicitly directs the assistant to the real existing file ("ada file foto namanya contohfile ... yang udah ada di folder public/images") and expects reuse of existing assets over creating new placeholders. Check which asset files actually exist before wiring up links/images. FRESH EVIDENCE: the same pattern recurred with the user's own photo set — after the assistant replaced the hero/news images with generated placeholder files (hero-1..4.jpg), the user pointed out the photos were gone ("kok malah ilangg fotonya") and directed the assistant to use the files THEY provided (rumbas.jpg, rumbas2/3/4.jpeg) for both the rotating hero background and temporary berita fallbacks — before generating or swapping in new image assets, check what image files the user has already supplied and use those. Confidence: 0.7
- When a data fix targets values baked into seeders (e.g., removing program-card icons defined in `DatabaseSeeder.php`), editing the seeder alone is NOT enough for an already-deployed database — `firstOrCreate`-based seeding never updates existing rows, so the deployed DB keeps the old data. Add an explicit, idempotent update/cleanup command to the deploy entrypoint as well (e.g., `Layanan::whereNotNull('ikon')->update(['ikon' => null])` in the tinker step) so the same fix applies automatically on Railway after push, and verify the local DB is also cleared (not just the seed file). Confidence: 0.7
- When berita/news cards STILL render with empty images after a fallback was wired in, first check the DB `gambar` values before touching the fallback code — here "17 Agustus" and "Rumah Belajar" actually had their OWN stored image paths (not NULL), so the rumbas fallback never applied; the real breakage was a missing `public/storage` symlink, so files that existed under `storage/app/public/berita` 404'd at the public `storage/berita/...` URL. The fix was `php artisan storage:link` locally AND ensuring the deploy (Dockerfile/entrypoint) runs it, otherwise the same empty images recur on Railway after push. When user-supplied images "still don't show", the item may be serving its own broken DB-stored image rather than the fallback pool — diagnose at the storage/serving layer before re-editing view logic. Confidence: 0.65
- When a client-side state-preservation fix (e.g., saving form data to sessionStorage on input/beforeunload) still fails on the user's real device, don't layer more client-side workarounds on top — eliminate the state-loss trigger itself. Here the "Lihat contoh surat" link opened in a new tab (`target="_blank"`), which on the user's mobile device navigates/reloads and wipes the register form despite the save/restore script, so the definitive fix was opening the example letter in a MODAL (iframe) on the same register page with no navigation at all — plus adapting the embedded page for the embedded context (back button becomes "Tutup" and closes via `window.parent.postMessage`, embedded-mode CSS), so no data (including chosen files) can ever be lost. CORRECTION (fresh evidence): the modal alone still wasn't sufficient — the user again reported "masih belum berubahh" after the modal was in place, because the saved state lived in sessionStorage, which is PER-TAB (data written in the register tab is invisible to other tabs), so returning from a letter opened in another tab still showed an empty form. The working fix combined the modal with switching the storage to localStorage (shared across ALL tabs), so the form data survives the sample-letter round-trip regardless of how the letter opens or which tab the user returns from. FURTHER CORRECTION (fresh evidence): even with the modal + localStorage in place, the embedded preview's arrow-with-text "Tutup"/"Kembali ke Pendaftaran" button was itself a plain link (`<a href=register>`) — clicking it navigated/reloaded the register page and wiped the FILE upload (text fields survived via localStorage restore), which the user reported as a precise split symptom ("yang dokumen yang udah aku upload tuh hilang, tapi yang lain masih ada"). The user rejected keeping any secondary close/back control inside the embedded content and asked to remove the arrow "Tutup" button entirely, leaving ONLY the modal's "X" close button (hides the overlay, no navigation) — the embedded preview must render no back/close link at all, and leftover hidden markup for it should be deleted, not just CSS-hidden. Confidence: 0.7
- Before an upcoming user-review / external-testing session ("aku habis ini mau user review"), the user wants member data reset to a clean slate so reviewers experience registering from scratch — and the reset must be narrowly scoped to exactly what was named ("member aja ya, jangan yang lain lain"): delete member accounts plus their dependent data only (pendaftaran, notifications, uploaded member-dokumen files); admin accounts and all content (berita, program, jadwal, layanan) must stay untouched, and the scope boundary is stated explicitly before executing. Confidence: 0.65
- Content images that must render on the deployed Railway site cannot rely on Laravel's `storage/` (per-instance, not committed to git) — berita photos that existed locally rendered empty/broken on Railway, and the user expects the deployed site to look the same as local ("ini samain yang udah dirailway yaa"). REVISED after a fresh 404 on Railway: git-tracked static images DO deploy, but admin uploads do NOT persist on Railway — the container is rebuilt from git on every deploy, so any photo uploaded at runtime into `public/images/berita` (or `storage/`) disappears → 404. The user hit exactly this when adding/changing a berita photo on the deployed site ("yang pas dirailway, aku mau ubah/tambahkan foto di berita... malah error muncul pesan 404") and expects admin uploads (add/change photos) to work on the deployed site just like locally. Accepted fix now: berita uploads are stored in the DATABASE as base64 data URIs (new TEXT column `gambar_data`), served via a `Berita` model accessor (`gambar_url`) that prioritizes `gambar_data`, strips legacy `berita/...` storage prefixes, and falls back to git-tracked static files — because the deployed PostgreSQL DB persists across redeploys, uploaded photos can no longer 404 or vanish. Confidence: 0.75
- Data lifecycle operations the user expects to run on the DEPLOYED site (e.g., resetting all member data before each user-review round) cannot be done from local tinker/scripts — those only affect the local DB, and the user will otherwise report that members are still there on Railway after a successful local reset ("pas dikode biasa bisa kehapus, tapi pas ku buka di Railway membernya masih ada"). Implement such operations as self-service in-app admin features (e.g., a "Reset Member" button — POST route + JS confirm dialog) that act on whatever database the deployed app is connected to, so the user can click it directly on Railway after each push/deploy without touching the DB manually. Confidence: 0.65
- When a stored value moves to a new DB column (e.g., berita image from `gambar` filename → `gambar_data` base64), update EVERY view's existence guard — `@if($item->gambar)` must become `@if($item->gambar_url)` — not just the rendered src/URL. After the migration nulled the old column, every view still testing `@if($item->gambar)` silently skipped rendering the image (detail pages showed nothing, listings fell back to generic placeholders) even though the model accessor returned a perfectly valid URL — grep all blade views for the old field in BOTH conditions and attributes before declaring the migration done. Confidence: 0.7
- Bulk destructive "reset/restart" features that span related-but-distinct data types must offer separate, granular options rather than a single combined button — the user explicitly asked to split the red "Reset Member" control into two choices: "Reset Pendaftar Program" (delete only program-enrollment records; member accounts stay) vs "Reset Pendaftar Member" (delete member accounts, which cascades to their pendaftaran/notifications/dokumen files). His stated rationale: a combined reset forces deleting everything when he may only want to clear program registrations ("misal aku cuma mau hapus pendaftar program aja, tanpa menghapus pendaftar member juga, kan nanti ngga bisa"), and he understands the cascade direction (deleting the registrant accounts automatically takes their program enrollments with them). Each option should be its own action with its own confirmation that spells out exactly what is deleted and what is preserved (member tetap tersimpan / admin & content untouched). Confidence: 0.8
- Bulk destructive "reset/restart" features that span related-but-distinct data types must offer separate, granular options rather than a single combined button — the user explicitly asked to split the red "Reset Member" control into two choices: "Reset Pendaftar Program" (delete only program-enrollment records; member accounts stay) vs "Reset Pendaftar Member" (delete member accounts, which cascades to their pendaftaran/notifications/dokumen files). His stated rationale: a combined reset forces deleting everything when he may only want to clear program registrations ("misal aku cuma mau hapus pendaftar program aja, tanpa menghapus pendaftar member juga, kan nanti ngga bisa"), and he understands the cascade direction (deleting the registrant accounts automatically takes their program enrollments with them). Each option should be its own action with its own confirmation that spells out exactly what is deleted and what is preserved (member tetap tersimpan / admin & content untouched). Confidence: 0.8
