# ui
- The homepage search bar should act as a site-navigation launcher, not a full-text content search: clicking/focusing it opens a dropdown listing only the website's actual menus (Lingkup Pelatihan, Berita, Program, Daftar Sekarang, Profil, Kontak), and clicking an item navigates straight to the corresponding section (anchor/hash, e.g., `/#pelatihan`, `/#kontak`) or page (e.g., `/register` for "Daftar Sekarang"). The dropdown must open immediately on click of the search bar — do NOT require the user to type first; show all menu options upfront so users who are confused about what to search can browse the choices (typing only filters the already-visible list). Confidence: 0.88
- Avoid duplicate flash/success alert messages — when a shared layout (e.g., `layouts/admin.blade.php`) already renders `session('success')`/flash messages globally at the top of every page, individual page views must NOT render the same flash block again; leftover per-page `@if(session('success'))` blocks make the alert appear twice (e.g., after approve/reject actions) and should be removed. Confidence: 0.75
- Call-to-action links (e.g., "Lihat selengkapnya") should be styled as solid button boxes with background color, padding, and border-radius — not plain text/underlined links — so they have visible weight and are not "polosan hanya tulisan". Confidence: 0.80
- Use flexible/inclusive labels for identity document form fields (e.g., "KTM / Kartu Pelajar / Identitas Lembaga Pendidikan") to accommodate various user types (students, ibu-ibu, etc.) rather than narrow, rigid labels. Confidence: 0.75
- On member-facing program listing pages, include a prominent "Daftar Program Sekarang" (Register Now) call-to-action button beneath the list so members can proceed to registration immediately after browsing. Confidence: 0.78
- Media/framed images (flags, logos, avatars) should NOT be force-cropped into a circular or any other clip shape — display them with their natural aspect ratio, intact, so nothing gets cut off ("ga bulett shape buat frame gambar nya... kalo bendera ya bendera png gak bulet sampek kepotong gitu"). In orbit/carousel components the images should have NO frame at all — no border, background, or rounded-rectangle box, just the bare natural image ("udah gausah dikasih frame biar bentuk asli gambar tersebut"); only the active/center-switched image may be shown circular. Confidence: 0.9
- When the user says something "jelek banget" (looks really bad) about a UI/animation, treat it as a full redesign request: re-ask/confirm the exact requirements first (via a question with concrete options, e.g., PNG flag files vs inline SVG), then rebuild the component from scratch rather than patching the existing one. Confidence: 0.75
- Generate missing image assets (e.g., country flags) as actual PNG files in the project's public images folder (generated via PHP GD) instead of inline SVG or CSS-only stand-ins — the user explicitly chose PNG files so images appear whole and uncropped like other assets. Confidence: 0.75
- For member-facing program registration flows, prefer a progressive disclosure pattern: show a summary overview on the listing page with a "Lihat Detail & Daftar" link → clicking opens a dedicated detail page showing full program description + related class schedules + a "Daftar Sekarang" button. Avoid placing direct one-click register buttons on program cards, as members should see complete context (especially current schedule) before committing to registration. Show a disabled "✓ Terdaftar" or "Kamu sudah terdaftar" state on the detail page for already-registered programs. Confidence: 0.80
- For member-facing program listing, use a two-page progressive disclosure pattern: a minimal summary listing page (showing only program names + icons, no descriptions) → then a dedicated detail page per program showing full description + related class schedules + registration form. The listing button should say just "Daftar" (not "Lihat Detail & Daftar") to keep it clean, with full context deferred to the detail page. Avoid combining everything (listing, all schedules, registration) onto a single monolithic page — members should first browse, then click into a specific program to see its context and register. Confidence: 0.85
- Include visible explanatory legends or contextual help text for domain-specific terminology (e.g., "Tematik" vs "Tentative") on user-facing pages so that non-expert members can understand the meaning without confusion — the user explicitly asked the program registration form to present the two class types as selectable options, each WITH a short explanation. CORRECTED definitions to show (per the mentor; the opposite of the earlier text): Tematik = one big theme/book studied over several consecutive meetings, register once and stay registered; Tentative = one topic/material per meeting, register per meeting, registration auto-expires at the next meeting/week. Confidence: 0.85
- When multiple related sub-items (e.g., "Visi" and "Misi") belong to the same conceptual entity, group them into a single container/card rather than rendering each sub-item in its own separate card — tightly related content blocks should be visually merged into one cohesive unit. This is an exception: for all other content items (non-visi_misi), each individual item should have its own separate card/container on listing pages, not all grouped into one box. Confidence: 0.80
- Card borders should be clearly distinguishable/visible against the page background — avoid overly subtle or barely-visible border colors (like very light gray) that make cards blend into the page. Use a medium-toned border (e.g., blue-gray `#d8e2ed`) with a subtle box-shadow to give cards clear visual separation and presence on the page. Confidence: 0.82
- Make identity/document form fields optional with contextual guidance text when they don't apply to all user demographics (e.g., KTM optional for non-students with note explaining when to fill vs skip). Confidence: 0.80
- Display user-uploaded profile photos (e.g., on member dashboard, admin detail pages) using the actual uploaded image file rather than a generic icon/fallback SVG. Confidence: 0.70
- Show an unread notification count badge (red circle with number) on a bell icon in the member dashboard header, and display recent unread notifications in a card on the dashboard itself. Confidence: 0.78
- Display class schedule quota/availability information (e.g., "👥 Kuota: {angka}") as a visible badge on member-facing schedule cards so members can see remaining capacity before registering. Confidence: 0.75
- On program detail pages, show a summary section displaying total kuota, total confirmed registrations, and remaining (sisa) kuota — with color-coded warning when sisa kuota is low or full. Confidence: 0.78
- After a member successfully registers for a program, show a prominent green success banner on the program detail page with a WhatsApp group invitation button (stylized with WhatsApp brand green #25d366) to immediately guide the member toward joining the group. Confidence: 0.80
- All buttons should have a vibrant/solid appearance by default (not faded or translucent), with zero hover effects — only after being clicked (active/selected/disabled state) should the button become muted/faded to indicate it has already been used. Confidence: 0.80
- Avoid duplicate/redundant action buttons — if the same action (e.g., edit) is already available via a per-item control on each card/row, do NOT add a separate global button for that action elsewhere on the page. Confidence: 0.85
- In orbit/carousel components, side images must be positioned so they stay INSIDE the orbit ring line — their centers should land within the ring's radius band (between the inner and outer ring), not near the container corners where they drift outside/too far from the orbit ("tolong revisi lagi jarak gambar nya agar tidak kejauhan jadi tetap di dalam garis orbit tersebut"). The center circle should also be proportionally compact on BOTH desktop and mobile — when the user says the center bundaran is too big ("kayanya juga kebesaran baik lewat desktop atau mobile jadi perkecil lagi"), shrink it across all breakpoints and keep the enlarged center/active state matched to the new size. Confidence: 0.7
- Default to simple, restrained, understated UI styling — avoid "alay"/flashy flourishes (decorative text buttons, exaggerated hover effects) that read as generic AI-generated design ("terlihat spirit design AI"); add ornamentation/animations only when the user explicitly asks for a premium effect. The user again rejected a dropdown styled with colored SVG icons, per-item arrows, and decorated hovers as "terlalu design AI" and asked for "simple elegant" — repeated confirmation of this standing preference. Fresh evidence: the user also rejected the "→" arrow characters in the pendaftar-program drill-down UI as "kaya ai banget" and asked for plain native arrows instead. Confidence: 0.85
- Navigation/action arrows in UI (e.g., drill-down back links, "enter" badges, list item arrows) must be plain and native — use a simple SVG left-arrow in standard back-button style for go-back links and plain text labels (e.g., "Buka" instead of "Lihat Kelas →") for enter/action buttons. Text arrow characters like "→" / "←" in badges and links look generic/AI-generated ("jangan kek gitu lah kaya ai banget, ganti ikon panah biasa aja buat go back atau enter nya") and were explicitly rejected in the Pendaftar Program drill-down. FRESH EVIDENCE: the preference applies app-wide, including member pages — when the user asked to change "semua gaya ikon panah di dalam halaman member" away from "panah template ai" to "ikon panah standar", the accepted fix was replacing every inline unicode arrow character ("Lihat Semua →", "Edit Profil →", "← Kembali ke Dashboard/Program") with a consistent standard Feather-style SVG arrow icon (14px, `stroke="currentColor"` width 2.5, line + polyline head, wrapped in an `inline-flex` with a small 4–6px gap from the label), verifying via grep that zero unicode arrow characters (`[→←]`) remain anywhere in the views. FRESH EVIDENCE (admin panel): the user repeated the same request for the admin pages — "tolong ubah tanda panah di semua section di halaman panel admin juga agar menjadi ikon panah default jangan seprti template begitu" — and the accepted fix was identical: replacing the unicode arrows in the admin dashboard (two "Lihat Semua →" links), the admin Program index ("← Kembali ke Program & Jadwal"), and the admin Member detail ("← Kembali ke Data Member") with the same standard Feather-style SVG arrow icons, then grepping `[→←]` across `**/admin/**` to confirm only HTML-comment occurrences remain. Non-arrow SVG icons (folder, download/upload) are left untouched. Confidence: 0.9
- Dropdown UI components (e.g., the homepage search-bar dropdown) should look simple, elegant, and native — not custom-styled with an obvious AI design. Concretely: use plain/native-style icons (like PHP native default icons rather than custom fancy SVGs), left-align the text inside the dropdown, and use a plain/simple arrow in the search bar instead of a decorative one — the user rejected the current design as "terlalu design AI" and asked to "ubah ikon menjadi ikon seperti bawaan php native". Confidence: 0.85
- Every edit/form page should include a visible "back" navigation control at the top so users can easily navigate back to the previous page without using browser back — but it should be a simple, understated circular back-arrow icon button (icon-only, subtle translucent background, gentle background-darken hover, keeping title/aria-label for accessibility), NOT a text link with decorative hover effects. The user called the "← Kembali ke Beranda" text-button style "terlalu alay dan terlihat spirit design AI" and asked for just "ikon back biasa" (a plain back icon). Confidence: 0.85
- When multiple action controls appear adjacent on card items, use semantically distinct icons (e.g., calendar for schedule, pencil for edit) rather than generic icons like arrows that could be misinterpreted as having the same function as nearby controls — consider adding text labels alongside icons to further disambiguate their purpose. Confidence: 0.80
- The admin dashboard should display a comprehensive set of summary stat cards — one count card for every major data entity (berita, profil, pendaftar, member, galeri, jadwal kelas, pesan) plus recent-activity lists (recent pendaftar, recent pesan) — so admins get a complete overview at a glance. Confidence: 0.70
- Member dashboards should include a registration status summary shown as colored pill badges with counts (e.g., ⏳ Menunggu / ✓ Terkonfirmasi / ✕ Ditolak) above the registration history, so members see their status at a glance without counting manually. SUPERSEDED for program enrollments: once program enrollment became instant-confirm (auto-reject only for full quota), the user explicitly had the Menunggu/Ditolak approval-summary pills removed from the member dashboard — each enrollment now shows its own badge ("Terdaftar" blue / "Kuota Penuh" red) plus a Batal Daftar button instead. Confidence: 0.4
- On member dashboards, show the member's OWN registered-class schedule as a weekly "Jadwal Kelas Mingguan" reminder card — the user explicitly requested a weekly schedule in addition to the registration history, as a reminder of which classes they've enrolled in ("selain history, harusnya ada jadwal mingguan si member untuk pengingat apa saja kelas yang sudah dia daftar"). Accepted pattern: card placed ABOVE the Histori Pendaftaran section, grouped per day (Senin–Minggu) in day-of-week order, showing only confirmed enrollments whose schedule date hasn't passed yet, each entry displaying date, class name, time in WIB, pengajar, plus jenis (tematik/tentative) and mode (online/offline) badges; empty state with a "Lihat Program" CTA. Confidence: 0.85
- Password input fields on auth forms (e.g., reset password) should include a show/hide visibility toggle button (eye icon) so users can verify what they typed before submitting. Confidence: 0.6
- When a member's account registration is rejected, the rejection banner/notification on the member dashboard should include a shortcut link (e.g., "Kirim Pesan ke Admin") that deep-links straight to the website's existing contact message form ("kotak pesan hubungi kami" in the homepage's kontak section, i.e., `route('home') . '#kontak'`) — pressing the button must take the member directly to that message box so they can appeal/complain/ask about their registration. Reuse the existing public contact form rather than building a new member-internal messaging page; the user explicitly rejected creating a new form/page when the existing website form already suffices. Confidence: 0.88
- The member dashboard should show a status banner at the top for every account status, with any admin note (catatan) visible there: pending = yellow "Akun Sedang Ditinjau" banner, approved with note = green "Pesan dari Admin" banner showing the note, rejected with note = red "Akun Ditolak" banner with the note — so the admin's decision/reason is visible at a glance, not only stored in the DB (user: admin notes on approve/reject "harusnya catatan muncul di bagian notifikasi atau bagian atas dashboard member"). Confidence: 0.8
- After a form submission (e.g., the contact/message form), the post-submit redirect should land back at the form's position via anchor/hash (e.g., `route('home') . '#kontak'`) rather than at the top of the page — so the success/error flash notification shown above the form is immediately visible and the user knows the message was sent without having to scroll up. Confidence: 0.80
- After a successful save on a member-facing edit form (e.g., "Simpan Perubahan" on Edit Profil), the user must NOT remain stuck on the form — redirect away to the main page (the member dashboard) plus a success flash notification (e.g., "✅ Profil berhasil diperbarui."), because leaving the user on the edit form after saving gives no signal that the change was actually stored ("dia tetep stuck di form edit profile... harusnya setelah klik simpan perubahan harus ada aksi kembali ke dashboard member agar menandakan perubahan berhasil tersimpan dan ada notifikasi nya"). The redirect + visible notification together are what marks the save as successful. Confidence: 0.8
- Alert/notification text (both success and error flash messages) on dark-colored backgrounds should be white (#ffffff) for high contrast and readability — avoid light-tinted text colors that blend into the dark background. Confidence: 0.70
- On light/white form or card backgrounds, alert message text must also contrast with the background — white text becomes unreadable there. Success/confirmation alerts should use the site's dark brand blue (e.g., #0c4e91) and error alerts a dark red (e.g., #b91c1c), never white (the user asked to change the "pesan berhasil dikirim" font from white to blue on the white contact-form card). Confidence: 0.75
- Prefers modern, interactive 3D Coverflow/Swiper carousels (e.g., for the homepage news section) over static card grids — the active center card should be largest (scale 100%, opacity 100%) with the highest z-index showing full detail, while side cards render smaller (scale 75–80%), dimmed/transparent, with perspective rotateY tilted toward the center. Confidence: 0.85
- Carousel slide transitions should animate very smoothly with ease-in-out easing (e.g., `cubic-bezier(0.4, 0, 0.2, 1)`), never abrupt jumps. Confidence: 0.80
- Interactive carousels should include a dynamic caption/description area below the cards that updates with the active slide (date + title + summary), not static captions. Confidence: 0.75
- Carousel navigation controls (Prev/Next) should sit beside the cards (left/right, vertically centered with the carousel) so they are immediately visible and clickable WITHOUT scrolling — the user complained that placing them below the carousel forced scrolling down to navigate ("kalau mau next atau back tuh harus scroll kebawah dulu"). Pagination dots go below the carousel, separate from the side buttons. Confidence: 0.82
- Interactive carousel/coverflow cards must be sized so the card AND its image are never clipped or cut off: card height should fit within the stage height at every position (active and side), side cards must not overflow the visible area, and sizes must be re-proportioned per breakpoint (desktop/tablet/mobile) — the user called the clipped news image "aneh dan gaenak banget". Confidence: 0.8
- Carousel caption text (title + description) should be center-aligned with a consistent spacing rhythm between elements (stage → caption → dots → CTA) — the user rejected imprecise spacing and awkward left/justified alignment in the news section ("ga presisi spacing nya trus rata kanan rata kiri nya kaya ga pass gitu lo jarak nya"). Use uniform gaps (e.g., caption margin 16px, dots margin 20px) and a max-width on captions. Confidence: 0.75
- CTA buttons and their icons must use readable sizes — e.g., the "Lihat selengkapnya" button font was bumped from 10px to 14px with matching icon size (10px → 14px) and balanced margins (32px top / 24px bottom) because 10px was too small and cramped ("kaya ga presisi"). Avoid tiny font sizes on prominent action buttons. Confidence: 0.75
- For viewing detail content (e.g., clicking a berita/news card), prefer an in-page interactive modal pop-out over navigating to a new page — the modal should fade in smoothly over a dark semi-transparent blurred overlay, have a dark background with light text, be internally scrollable, and include a clear 'X' close button in the top-right corner (closing also via overlay click or Escape) so the user returns to the previous view without leaving the page. Confidence: 0.85
- Wrap major content sections (e.g., "Lingkup Pelatihan" with its cards and CTA) in a large container box with a light-blue gradient background (`linear-gradient`), very rounded corners (`border-radius: 32px`, reduced responsively on mobile), and generous padding so the section stands apart from the main page background; headings/description text inside the colored container should be white for contrast, with cards arranged on a responsive CSS Grid (3 → 2 → 1 columns) and CTA restyled to match the new background. Confidence: 0.78
- Pages reached via "Lihat selengkapnya" links from the homepage sections (e.g., /layanan, /berita, /profil — both listing AND detail/show pages) should not display the footer; only the homepage keeps it. Implement by wrapping the footer in the shared layout with a flag (e.g., `@if(!isset($hideFooter) || !$hideFooter)`) and passing `$hideFooter = true` from the relevant controllers. Confidence: 0.85
- Adjacent major sections that sit directly on top of each other (e.g., the footer and the "Hubungi Kami" section above it) must have visually distinct background colors so they don't blend together ("jangan menyatu") — but the colors should stay in the same family and harmonious ("beda tapi tetap nyatu", "jangan terlalu nabrak"), e.g., light blue section above vs. dark navy footer. Confidence: 0.75
- Footer content (Kontak & Alamat, Program & Pendaftaran, Peta Lokasi) should be laid out as equal columns in a single row on desktop — the map inline with the other columns rather than stacked below them — collapsing to fewer columns (2 → 1) responsively on smaller screens. Confidence: 0.7
- The copyright line at the very bottom of a footer should be presented as its own separate box/container (own darker background, rounded corners, border, logo + text) visually distinct from the main footer content above — not a plain text line merged into the contact section. Confidence: 0.7
- Prefer dark navy/slate tones over bright/light blues for footer backgrounds so the footer stands apart from lighter sections above it, while remaining in the same blue family. Confidence: 0.55
- Section backgrounds AND form components (form container box, input fields, labels, submit button) must be harmonized with the site's overall color palette so nothing clashes ("nggak nyatu", "jangan terlalu nabrak") — e.g., the "Hubungi Kami" section was changed from a bright light blue (#56b0ff) that clashed with the site's dark-navy palette to the same dark-blue gradient used by the other CTA sections, with the form box restyled as a white card and inputs as clean light fields on that navy background. Colors should feel cohesive with the rest of the page, not stand out as mismatched. Confidence: 0.72
- Decorative transition elements (e.g., SVG wave separators) between sections must keep the wave shape clearly visible — do NOT hide/merge the wave so completely that the curve disappears. The user explicitly rejected an approach that embedded the wave SVG inside the section to make it one continuous element, because the wave then became invisible ("lohhh gelombang gelombangnya jangan dihilangkan, tetap kasih gelombang gelombang"). The accepted pattern: the wave remains a distinct, clearly-visible curve element sitting between sections, but its fill color must EXACTLY match the section it flows into (e.g., same `#0c4e91` as the "Hubungi Kami" section) so it reads as a natural, seamless extension of that section while the wave silhouette stays apparent. Color mismatch between wave and adjacent section is rejected ("yang selombang gelombangnya sama background itu masih belum sama warnanya"); eliminating the visible wave shape is also rejected. Confidence: 0.88
- Avoid flat-looking UI containers/cards (e.g., the "Hubungi Kami" form box): style them dimensionally/premium with a subtle gradient background (white fading to a light blue tint), layered box-shadows plus a soft glow halo, rounded corners (~24px), a gentle continuous floating animation (e.g., `formFloat` translateY loop ~6s ease-in-out), and an intensified glow on hover — the user explicitly asked for animations/shadow/gradient on the container because it felt too flat ("biar ngga flat gitu, kasih shadow/ gradasi/apa gitu"). Confidence: 0.8
- Reuse the homepage's anchor blue (`#0c4e91`) for standalone CTA-style section backgrounds (e.g., "Hubungi Kami") so sections feel like one cohesive blue family and nothing clashes ("jangan nabrak"). Exception: a section immediately below the hero/beranda that must read as one continuous block uses the canonical bright hero blue gradient `#56b0ff → #8fc9f9 → #337fc1` instead — `#0c4e91` was explicitly rejected there as too dark ("backgroundnya jangan terlalu gelap, warnanya samakan kayak yang bagian beranda"). Confidence: 0.8
- When a section needs visual layering, prefers a nested color hierarchy within the same blue family: outer section background = the homepage beranda/hero medium blue gradient (kept light-to-medium, not too dark — see above), an inner container box = white with a soft blue shadow, and the item cards inside = a darker blue gradient (e.g., `#0f5a9e → #0c4e91 → #0a3f7a`) with light/white text so content stays readable — all colors adjusted to harmonize ("sesuaikan sama yang lain agar bisa menyatu, jangan nabrak"). Confidence: 0.8
- For a section below the hero/beranda that must read as one continuous blue block (user: "masih kurang sama... samakan biar jadi kayak 1 satu gitu"), match the CANONICAL hero blue gradient (`#56b0ff → #8fc9f9 → #337fc1`) — do NOT derive the color by sampling rendered pixels across the seam. Pixel-sampling the hero's actual rendered output picked up a greenish photo tone from the hero slideshow and made the section look GREEN, which the user emphatically rejected ("WARNA BIRU IHHH, KO JADI WARNA HIJAUUUUU, WARNA BIRU KAYA YANG PERTAMA DULU ITULOHH"); the fix was reverting to the original bright blue gradient. Use the intended brand/CSS blue values, not pixel samples of photo-covered areas. Confidence: 0.9
- On the member program detail page, display the current week's class schedule directly below the program description card (and above the Daftar button) so members can verify the schedule fits / doesn't clash with their personal schedule before registering — each program's schedule appears under its own detail page (Bahasa Jepang's schedule under Bahasa Jepang's detail, etc.), never combined into one shared list. Confidence: 0.85
- When a program has no schedule for the current week, the "Daftar" button on the member program detail page should be replaced by a disabled "Daftar Tidak Tersedia" state (grayed out, not-allowed cursor) with explanatory text that the admin hasn't set the schedule yet — and program cards on the listing page should show a "Jadwal belum tersedia" badge with a disabled button too. Confidence: 0.85
- Custom error pages (404, 500, and common user-facing ones like 403/419) should be built in `resources/views/errors/` to match the site's design language (brand blue/teal colors, white rounded card, consistent button style, `noindex` meta) instead of leaving Laravel's default error pages — the user considers the defaults ugly and information-leaking ("jelek & bocorin info"), especially during user testing. Confidence: 0.75
- Form fields labeled as accepting multiple contact formats (e.g., "Email/ No Telepon") must actually accept both — the input should use `type="text"` and backend validation should drop the `email` rule (e.g., `required|max:255`) so entering a phone number without "@" isn't rejected, and the placeholder should show both examples (e.g., "contoh@email.com / 0812345678"). Validation behavior must match what the label promises: the user complained "saat input no telp kok masih harus kasih @ wkwkwk" when the field still enforced email format. Confidence: 0.8
- In image-card layouts (e.g., berita/news coverflow cards with a photo on top), text content (date + title) must sit close to the image with tight, compact spacing — avoid excessive whitespace between image and text ("whitespace antara font dan gambar too much"). Keep the card body's top padding small (e.g., 16px → 12px) and the gap between date and title minimal (e.g., 6px → 4px) so the card reads as one dense, proportional unit. Confidence: 0.65
- The member dashboard header should NOT include a "go back" icon or "Kembali ke Beranda" link/button — the dashboard is a top-level page, so its header should stay clean with only the page title, the notification bell, and Logout. Back-navigation controls belong on edit/form/detail sub-pages reached from a listing, not on main dashboards; the user explicitly asked to remove the back-to-home button from the member dashboard for all member statuses (pending and approved alike). Confidence: 0.72
- Form validation errors must be marked in red following standard UX laws (user: "jangan lupa notif nya warna merah ya sesuai ui ux laws"): error notification text in red, AND the offending input field highlighted with a red border + light red-tinted background (e.g., border `#dc2626`, background `#fef2f2`), with a red error message rendered directly beneath the field — so users immediately see which field caused the rejection, not just a top-of-page alert. This per-field inline error pattern should be applied to EVERY form field (name, email, password, confirmation, phone, address, file uploads, selects), not just email. Validation should also combine server-side rules with HTML5 attributes (`required`, `type`, `accept`) so the browser gives real-time feedback before submit. Confidence: 0.78
- Form input placeholders should show example values rather than instruction text — e.g., `contoh@email.com`, `08xxxxxxxxxx`, `Contoh: Budi Santoso`, `Contoh: Jl. Ahmad Yani No. 1, Surabaya` — not "Masukkan email" / "Masukkan nama lengkap". Keep placeholder wording consistent across all forms (login, register, kontak, edit) so users recognize the expected format ("format contoh, bukan instruksi"). Confidence: 0.68
- Empty states on listing/dashboard pages should be friendly and visual rather than plain text: a centered icon inside a soft circular background, a short bold title (e.g., "Belum ada notifikasi"), a brief explanatory subtitle, and — where a natural next action exists — a CTA button linking to it (e.g., "Lihat Program" on the member dashboard when there are no registrations yet). Confidence: 0.62
- For decorative visual elements in hero/about sections (e.g., the About section illustration), prefers rich interactive animated components over simple static/looping ones — e.g., an orbit/radar pulse animation: concentric dashed/solid rings rotating slowly in opposite directions, a glowing bright cyan/blue center circle with a pulsing ripple (glow + scale keyframe), and images placed at orbit points that automatically swap into the center on a periodic timer (every few seconds) with very smooth fade + scale transitions — the user explicitly asked to replace the spinning-house SVG with this orbit animation. The orbit design was then refined after rejection ("aduhh jelek banget animasi nya"): (1) orbit items sit at the four CORNERS (top-left / top-right / bottom-left / bottom-right — diagonal), NOT centered on the straight top/right/bottom/left axes ("layoutnya... agak serong, ga lurus"); (2) each orbit slot holds a DIFFERENT image (country flags as real PNG files + brand logos) rather than repeating the same logo in every slot ("ga logo ruma bahasa doang tapi bendera indonesia bendera jepang bendera inggris"); (3) items stay SMALL at their corner positions and only ENLARGE when the active one switches into the center ("gambar membesar itu ketika dia switch ke tengah aja, kalo ke samping... jadi mengecil"); (4) exactly THREE images should be VISIBLE at any one time — one center slot + two side slots (left/right) — while the full pool of 5 image files (3 flags + 2 logos) cycles through those slots automatically like a carousel; the user clarified "gambar nya tuh 3 aja... emang file nya 5 gambar tapi ya gausah ditampilin semua... bisa otomatis switch yang tampil tapi cuma 3 yang tampil" when the implementation displayed all 5 at once ("GAMBAR NYA 3 AJAAAAA") — never show the entire asset pool simultaneously in a rotating display; (5) orbit items must have NO frame/border/background — the bare image in its natural shape, since the rounded-rectangle frame was explicitly rejected ("kok malah ganti frame kotak sih?? udah gausah dikasih frame biar bentuk asli gambar tersebut") — only when the active image switches into the center may it be displayed circular ("kecuali kalo ketengah itu jadi bulet enggak papa"); (6) for the reduced 3-visible-slot version, position the side slots DIAGONALLY at top-right and bottom-left flanking the center (user: "aku maunya di atas pojok kanan, tengah, dan bawah pojok kiri") — not horizontally at left/right mid-height; (7) the visible images must ALWAYS be distinct from one another — when a side slot's image moves into the center, that vacated slot must be immediately refilled with the next image from the rotation pool so no duplicate/"double" image is ever visible mid-transition. The user reported this bug TWICE ("kok gambar yang ditengah sama di pinggir kanan sama? jadi kaya double" then again "gambar nya masih double antara yang di tengah sama yang di pinggir... tolong ketiga gambar yang tampil tuh dibuat beda semua biar enak dilihat dan ga double picture") — naive sequencing (e.g., plain `counter % N` for the incoming slot) still produced duplicates on later ticks because the newly chosen index could equal an index just moved into another slot. The accepted fix that finally resolved it: explicitly track which image index is currently displayed in EACH slot (a state object like `{tengah, atas, bawah}`) and have the "next image" selector check all currently-displayed indices, picking only an index not in use — guaranteeing all visible slots differ at every moment. Confidence: 0.92
- When the rotation pool of an orbit/rotating image display grows (e.g., from 5 to 14+ items after adding all country flags), the initial slot assignments and any hardcoded default indices must be updated to stay consistent with the new array — the assistant reset the initial state to show logo + a flag (e.g., `{ tengah: 0, atas: 1, bawah: 2 }`) and synced the static HTML defaults with the JS init after changing the pool, because the old defaults (which pointed at now-shifted array positions) would otherwise show the wrong image on load. Comments describing the pool size/count ("5 gambar") must also be corrected whenever the image count changes so inline documentation stays accurate. Confidence: 0.5
- When a registration form offers mutually-exclusive options that depend on what the admin has configured (e.g., class type "Tematik" vs "Tentative" on the member program-daftar form), options with NO corresponding schedule/data must be visibly disabled rather than left clickable or hidden: grayed out (reduced opacity + gray background), radio disabled, cursor `not-allowed`, plus a small note like "Tidak tersedia untuk program ini". If the admin configured both types, both options are enabled; if only one, only that one is enabled. The accompanying schedule dropdown should also be filtered (via JS) to show only schedules matching the selected type. The backend (controller) should compute the set of available types from the actual configured data and pass it to the view. Confidence: 0.8
- Program/class cards (e.g., Kelas Komputer, Kelas Bahasa Jepang) should be displayed WITHOUT decorative per-card icons — the user asked to remove the computer icon and the concentric-circle icon ("hapus ikon komputer dan bulat itu ... udah gausah dikasi ikon") and keep the program cards clean. Default `ikon` to null (no SVG icon in seeds/database) rather than attaching icon markup per program. Confidence: 0.65
- In multi-option enrollment forms (e.g., class type "Tematik" vs "Tentative" on the member program-daftar page), being registered for ONE option must NOT disable the whole registration container/form — the member stays free to register for other options in the same program (e.g., already registered Tentative → Tematik remains selectable) and the register button/container for other types must NOT be removed ("jgn di hilangkan tombol daftar nya"). The already-registered option must NOT be grayed out / dimmed either — the user explicitly rejected the gray treatment: "jangan matikan container daftar program kelas yang sudah terdaftar jangan dibikin abu abu". Correct pattern: an already-registered option keeps its NORMAL visual appearance (white background, full opacity), only its radio is disabled (default/not-allowed cursor) plus a clear "✓ Sudah terdaftar" badge, so the member sees at a glance which class is taken and which is still open ("cuman kasih aja kejelasan mana yang udah kedaftar jadi gabisa daftar lagi dan mana kelas yang belum"). Gray-out (reduced opacity + gray background) applies ONLY to options that are NOT available/configured by the admin ("Tidak tersedia untuk program ini"); the "sudah terdaftar di semua kelas" message appears only when the member has registered for EVERY available option. The backend should compute the set of already-registered types (array, e.g., `$jenisTerdaftar`) rather than a single boolean, and pass it to the view so per-option states are derived there. Confidence: 0.88
- The "Terdaftar" (already registered) state on member-facing program listing buttons must be GREEN (`#16a34a`, white text) and remain fully clickable (`cursor:pointer` with a darker hover) — NOT gray/abu-abu, because a gray disabled-looking button makes users think it can't be clicked when it actually is (it navigates to the program detail to view other classes). Green is specifically chosen over blue so it doesn't get confused with the blue not-yet-registered "Daftar" button. The user emphasized this emphatically: "dilahaman member kan ada BUTTON TERDAFTARRRRR?? JANGAN DIBIKIN ABUU ITU BIKIN USER NGIRA GABISA DI PENCETT ... GANTI WARNA NYA JADI HIJAUU AJA DEH BIAR GA BIRU BIKIN KELIRU SAMA YANG BELUM TERDAFTAR JUGA." Gray/`cursor:not-allowed` is reserved only for genuinely unavailable states (e.g., "Jadwal belum tersedia"), not for the already-registered state. Confidence: 0.85
- The "already registered" / disabled state of member-facing program registration containers must be computed DYNAMICALLY from the CURRENT active schedule, not frozen at the moment of first registration. Because schedules are weekly and auto-deleted once their date passes, a member who registered for a previous week's class must NOT remain permanently grayed-out/"mati" (dead): when the admin deletes the old schedule and adds new ones, the container must become active again ("spirit on") so the member can open it and register for the new week's schedule; only when the admin hasn't set any schedule yet should the card show the "Jadwal belum tersedia" state. The user reported that a class registered "kemarin" was still abu-abu and dead even after new schedules were added, insisting "harusnya berubah sesuai keadaan" — registration container states must track current schedule availability, not past registration history. Confidence: 0.8
- Back-navigation controls on member sub-pages (Program, Jadwal, Notifikasi, Edit Profil, Detail Program) must be placed at the TOP edge of the page — a back arrow at the top-left of the header beside the page title — and never at the bottom of the content area. The user explicitly asked to move every "Kembali ke Dashboard" button up ("ubah posisi tombol Kembali ke dashboard itu di atas pinggir, kalo dibawah user gabakal tau dn keliatan") because bottom placement is invisible/undiscoverable to users. Each sub-page should carry exactly ONE back control at the top: duplicate bottom text links ("← Kembali ke Dashboard"/"daftar program") and redundant header text links ("Dashboard" next to Logout) should be removed, and the back arrow's destination should be the logical previous page (dashboard for top-level member sections, the program list for Detail Program). Confidence: 0.9
- Label/value info rows (e.g., the member dashboard "Data Diri" rows with a label left and value right) must never let long user input (e.g., a long address) overlap/collide with its label — the user reported the address text "nabrak dengan judul text 'ALAMAT'" when users enter long addresses. The accepted fix: `align-items: flex-start` with a fixed gap (16px) so the label sits at top, and the value side gets `min-width: 0` + `flex: 1` + `word-break: break-word` so long text wraps down within its own column instead of crashing into the label. For genuinely long values (like addresses), use a dedicated modifier class (e.g., `--long`) with `text-align: left` and relaxed `line-height` for readability, while short values keep their right alignment. Layout must be robust to arbitrarily long user-supplied text everywhere, not just the reported field. Confidence: 0.75
- Homepage CSS animations (hero text, card pop-outs, title effects, floating boxes) must be SLOW and clearly readable — when the user says an animation is "terlalu cepat dan kurang smooth" (too fast and not smooth enough) or that a motion "ga keliatan" (can't be seen), lengthen durations (multi-second cycles: 1.2s → up to 3.5–5s), use slow-in/out easing (e.g., `cubic-bezier(0.22, 1, 0.36, 1)`), and increase the movement amplitude so each phase is perceptible. All decorative animations must respect `@media (prefers-reduced-motion: reduce)` (disable the animation and restore full opacity) for accessibility. Confidence: 0.85
- Homepage hero content should animate on page load with a staggered entrance that feels "senada" (in harmony) with the background animation: when the background slideshow zooms IN (Ken Burns, scale 1 → 1.08), the hero text (badge → title → description) zooms OUT (scale ~1.18 → 1 with fade, sequential delays ~0.2s apart, smooth cubic-bezier easing) for a cinematic counter-movement — the user explicitly requested the "Selamat Datang" text zoom out on page open, matching the background's motion. The hero SEARCH BAR must NOT participate in the entrance/loop animation — it stays fully visible and usable at all times (override with `opacity:1 !important; animation:none !important`), because a search column that repeatedly fades out breaks usability ("search bar ... jangan di animasikan juga, kan itu buat kolom pencarian masa hilang muncul huhu"). FRESH EVIDENCE that this is a general rule: exclude every functional/interactive element (search inputs, buttons, forms) from decorative show/hide animations — restrict loop animations to non-interactive content (badges, headings, paragraphs). Confidence: 0.82
- Card sections (e.g., "Lingkup Pelatihan") should reveal their cards one-by-one as the user scrolls to them — a pop-out effect (translateY + scale 0.85 → 1 + fade) with a per-card stagger delay (e.g., 0.12s × index) triggered by an IntersectionObserver adding a `.show` class — instead of all cards appearing at once. Confidence: 0.82
- Section-title animations (e.g., "Berita & Info Terkini") should be smooth but clearly perceptible — the accepted pattern is a flowing gradient shimmer (linear-gradient with `background-size: 200%` and a `background-position` loop, ~4s linear infinite) so the title visibly moves without being jarring; the user asked for motion that is "smooth tapi tetap kelihatan kalo bergerak" (smooth but still visibly moving). Confidence: 0.75
the same effect everywhere. Confidence: 0.7
- Brand showcase visuals (e.g., the homepage "Tentang Rumah Bahasa" orbit) must show the brand logo exactly ONCE — do NOT cycle two logo variants (e.g., `logo-rumbas.jpg` AND `logo.png`) through the same rotating display; the user flagged the duplicate ("eh logo rumah bahasanya 1 aja, itu ada dua") and the accepted fix removed the second logo file from the rotation pool so a single canonical logo sits at the center with only country flags as companions. Confidence: 0.7
- When asked for alternative presentation models of an existing visual component, the user picked the rotating "flag ring" (cincin bendera) model over bubble-rise and static-grid alternatives: one centered logo with ALL available language flags arranged evenly around it (each placed via CSS `calc()` using per-item `--i`/`--n` custom properties), the whole ring rotating slowly in pure CSS (e.g., 60s per revolution, pause on hover) — replacing the earlier JS slot-swapping orbit. Showing the entire flag set simultaneously is desired in a ring layout (unlike the earlier slot-swap layout where only a few items should ever be visible); motion is delegated to CSS keyframes rather than JS interval timers. Confidence: 0.6
- In rotating ring/orbit animations (e.g., the flag ring around the Rumah Bahasa logo), items that ride the ring (flags) must stay upright/axis-aligned while orbiting — give each item a reverse counter-rotation with the SAME period as the ring's spin (e.g., a `flagsRotateReverse 60s` animation on each flag image countering the ring's `60s` rotation, both pausing on hover) so nothing appears tilted/canted ("jangan dipencengin ... sejajar aja"). Any decorative reference ring the items are meant to sit on (e.g., the dashed `orbit-ring-1` of dots) must rotate at EXACTLY the same duration as the item ring — mismatched periods (40s vs 60s) make the flags drift off the dots ("gak sesuai sama titik titiknya") — and each item's placement radius must match that ring's actual radius so items sit ON the line. Flag/item sizes in such decorative rings should be proportional to the ring (reduced from 54px → 42px desktop / 30px mobile) so they read as tidy ornaments rather than oversized content. Confidence: 0.65
- When the user asks to "samakan" (make the same) sibling containers/cards on one page (e.g., the member dashboard's biodata/profil, jadwal kelas mingguan, and histori cards), they want the containers visually IDENTICAL: unified padding (all set to 24px), equal width, consistent heading treatment, and consistent gaps between them. When offered arrangement options for three such cards, the user chose the "all full-width stacked" layout (single column, uniform width) — the most consistent and safest-responsive option — over mixed two-column arrangements (e.g., profil-left with jadwal+histori right, or a full-width jadwal under a two-column row). Such card layouts must also stay responsive with no overflow when checked via browser devtools ("pas di inspect bisa responsif"): collapse to one column and shrink gaps/padding at small breakpoints (e.g., ≤768px gap 20px, ≤480px tighter padding). Confidence: 0.7
